machine:
  services:
    - docker

dependencies:
  pre:
    - docker version
    - make deps
  override:
    - make build

test:
  override:
    - tests/util/shunit2:
        parallel: true
        files:
          - tests/*.sh
